// Widget carte cocktail réutilisable (affiché dans les grilles de liste)
// - Image du cocktail avec cached_network_image + placeholder skeleton
// - Nom du cocktail
// - Icône favori superposée (top-right)
// - Hero tag basé sur l'id pour l'animation de transition vers le détail
// - Gère le tap → navigation vers CocktailDetailScreen
// - Taille adaptative selon le contexte (grille mobile vs tablette)
