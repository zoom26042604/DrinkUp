// Widget skeleton loader générique (effet shimmer pendant le chargement)
// - Variante carte cocktail : rectangle image + lignes de texte
// - Variante liste : plusieurs tuiles avec lignes animées
// - Animation shimmer (dégradé qui défile de gauche à droite)
// - Remplace les vraies données pendant l'état isLoading des providers
